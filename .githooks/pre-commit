#!/bin/sh
# Prevent commits directly to main or master branch
BRANCH="$(git rev-parse --abbrev-ref HEAD)"
if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "master" ]; then
  echo "✋ 直接 main/master へのコミットは禁止されています。develop または feature/* ブランチで作業してください。"
  exit 1
fi
